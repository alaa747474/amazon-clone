import 'package:flutter/material.dart';


class LoggedInCartScreen extends StatelessWidget {
  const LoggedInCartScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Logged in cart'),);
  }
}